<div class="todo-container">
    <h2>My Todo List</h2>
    <%= form_with(url: "/todos/", method: "POST", class: "input-container") do %>
      <%= text_field_tag "todo_text", nil, placeholder: "Enter a Todo" %>
      <%= text_field_tag "due_date", nil, type: "date" %>
      <input class="btn-add" type='submit' value="Add" />
    <% end %>
    <%= render partial: "todo-section",
               locals: { section_title: "Overdue", todos: @todos.overdue, show_due_date: true } %>
    <%= render partial: "todo-section",
               locals: { section_title: "Due Today", todos: @todos.due_today, show_due_date: false } %>
    <%= render partial: "todo-section",
               locals: { section_title: "Due Later", todos: @todos.due_later, show_due_date: true } %>

</div>